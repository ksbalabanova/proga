#include <iostream>

int main() {
    double x, y;
    int count_all = 0;
    int count = 0;
    double ans;
    while () {//не знаю как читать из потока
        if (((x * x) + (y * y) <= 2) && ((y + x * x )>= 2)) {
            count++;
        }
        count_all++;
    }
    ans = count / count_all;
    std::cout << ans;
}
